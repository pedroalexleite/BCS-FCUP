#include <stdio.h>
#include <ctype.h>
#include <string.h>

int todos_letras(char str[]){

for(int i = 0; str[i] != '\0'; i++){
str[i]=toupper(str[i]);
}

if(str[] == isdigit()){
return 0;
}
else
return 1;

}
int main(){
char str[] = "Abb34";

todos_letras(str);


}
